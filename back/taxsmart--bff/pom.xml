<?xml version="1.0" encoding="UTF-8"?>
<!-- This is a Project Object Model (POM) file for a Maven project. It defines the project structure, dependencies, build plugins, and other project-specific configurations. -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <!-- The parent section defines the parent project from which this project inherits configurations. -->
    <parent>
        <groupId>com.jao.taxsmart</groupId>
        <artifactId>taxsmart</artifactId>
        <version>${revision}</version>
    </parent>

    <!-- The artifactId element defines the unique base name of the primary artifact being generated by this project. -->
    <artifactId>taxsmart--bff</artifactId>

    <!-- The properties element defines project-wide properties that can be referenced elsewhere in the POM. -->
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <springdoc-openapi.version>2.5.0</springdoc-openapi.version>
        <openapi-generator.version>7.2.0</openapi-generator.version>
    </properties>

    <!-- The dependencies element defines the dependencies that this project has on other projects. -->
    <dependencies>
        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- SpringDoc OpenAPI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc-openapi.version}</version>
        </dependency>
    </dependencies>

    <!-- The build element defines the build configurations for the project. -->
    <build>
        <!-- The plugins element defines the build plugins used by the project. -->
        <plugins>
            <!-- Spring Boot Maven Plugin -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <!-- Maven Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <!-- OpenAPI Generator Maven Plugin -->
            <plugin>
                <groupId>org.openapitools</groupId>
                <artifactId>openapi-generator-maven-plugin</artifactId>
                <version>${openapi-generator.version}</version>
                <executions>
                    <execution>
                        <id>in</id>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <output>${openapi.generation.directory}</output>
                            <inputSpec>${project.basedir}/src/main/resources/openapi/in/openapi.yml</inputSpec>
                            <packageName>${project.groupId}.bff</packageName>
                            <generatorName>spring</generatorName>
                            <generateSupportingFiles>false</generateSupportingFiles>
                            <configOptions>
                                <additionalModelTypeAnnotations>
                                    @lombok.Builder;
                                    @lombok.With;
                                </additionalModelTypeAnnotations>
                                <openApiNullable>false</openApiNullable>
                                <interfaceOnly>true</interfaceOnly>
                                <useSpringBoot3>true</useSpringBoot3>
                                <library>spring-boot</library>
                                <skipDefaultInterface>true</skipDefaultInterface>
                                <apiPackage>${project.groupId}.bff.application.in.rest.controller</apiPackage>
                                <modelPackage>${project.groupId}.bff.application.in.rest.dto</modelPackage>
                                <useResponseEntity>false</useResponseEntity>
                            </configOptions>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>