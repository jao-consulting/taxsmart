#!/usr/bin/env sh

# This script is used to start the back-end server of the application.

# The java command is used to start the server. The server code is packaged in the app.jar file.
java -jar app.jar