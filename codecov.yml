coverage:
  status:
    project:
      default: off
      front:
        flags: front
      back:
        flags: back
ignore:
  - "back/**/*Application.java"

flags:
  front:
    paths:
      - front/
  back:
    paths:
      - back/